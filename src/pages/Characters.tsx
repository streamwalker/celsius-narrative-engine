import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Characters() {
  return (
    <PlaceholderPage
      title="Character Library"
      tagline="° CHARACTER LIBRARY"
      description="Browse, group, and export your cast of characters with consistency scoring and relationship maps."
      sourceLineCount={1107}
    />
  );
}
