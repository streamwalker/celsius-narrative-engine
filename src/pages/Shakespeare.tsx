import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Shakespeare() {
  return (
    <PlaceholderPage
      title="Shakespeare"
      tagline="° REFERENCE"
      description="Shakespearean structure, rhetoric, and scene patterns — annotated for modern writers."
      sourceLineCount={438}
    />
  );
}
